# Cerberus IAM admin console example environment
NEXT_PUBLIC_IAM_API_BASE_URL=http://localhost:4000
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Optional default organisation slug to send as X-Org-Domain when none is selected yet
NEXT_PUBLIC_DEFAULT_ORG_SLUG=cerberus-iam
# Optional internal URL for server-to-server requests (fallbacks to NEXT_PUBLIC_IAM_API_BASE_URL)
IAM_API_URL=http://localhost:4000
